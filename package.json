{
  "name": "hello-world-nodejs",
  "version": "1.0.0",
  "description": "Mercury Composable Node.js hello-world (direct + flow styles)",
  "type": "module",
  "engines": {
    "node": ">= 20.18.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node copy-resource-files.js",
    "start": "node dist/main.js",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "mercury-composable": "file:../mercury-nodejs"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "tslib": "^2.8.1"
  }
}
